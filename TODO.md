# 실서비스 개시 전 남은 위험

이 문서는 코드 리뷰와 실제 Postgres 검증을 거친 뒤에도 **남아 있는** 항목만 담는다.
이미 해결된 것은 git 로그와 `README.md`의 운영 가이드를 볼 것.

각 항목은 `[ ]` 상태 · 왜 위험한지 · 무엇을 해야 하는지 순으로 적는다.
코드로 해결할 수 없는 것(계약·법무·인프라 결정)은 ⛔ 로 표시했다.

## 한눈에

전체 32개 중 **5개 완료(`[x]`), 27개 남음.** 완료된 항목도 왜 문제였는지가 남아 있어야
같은 실수를 반복하지 않으므로 지우지 않고 표시만 바꿨다.

| 구분 | 남음 | 성격 |
|---|---|---|
| 🔴 개시 차단 (1~5) | 5 | **대부분 코드 문제가 아니다.** 4건은 법무·계약·인프라 결정(⛔). 2·3번은 초안까지는 나왔고 빈칸 채우기와 법무 검토만 남았다 |
| 🟠 개시 직전 (6~15) | 6 | 11·12·13·15번 완료. 남은 것은 배포 시 설정(웹훅 URL 등록, CSP 전환, 마이그레이션 확인)과 알림 미구현 |
| 🟠 UX (16~22) | 7 | **기능은 있는데 화면에서 도달·확인이 안 되는 것들.** 기능 테스트로는 안 잡힌다. 16~21은 주문 관리 화면 하나로 함께 해소된다 |
| 🟡 부채 (23~32) | 9 | 26번 완료. 나머지는 기능 확장 시점에 함께 처리 |

**"코드에 남은 결함"에 해당하는 건 사실상 없다.** 리뷰에서 나온 결함은 모두 수정하고
실제 Postgres로 검증했다. 이 목록은 (a) 사람이 결정·계약해야 하는 것, (b) 배포할 때
한 번 해야 하는 설정, (c) 아직 만들지 않은 기능 — 세 가지다.

**개시 전 반드시 읽을 것: 8번** (마이그레이션이 운영 데이터를 변경한다).

---

## 🔴 개시 차단 — 이걸 안 하고 열면 안 된다

### [ ] 1. ⛔ 법정 표시사항이 전부 더미값이다

`src/components/Footer.tsx:29-34` 가 지금 이렇게 출력한다.

```
(주)타이어존 / 대표이사 김대표 / 사업자등록번호 000-00-00000
통신판매업신고 제0000-경기000-00000호 / 개인정보관리책임자 : 관리자(admin@tirezone.example)
경기도 어딘가시 어딘가로 123 / 고객센터 : 1588-0000
```

전자상거래법 제10조는 상호·대표자·주소·전화번호·이메일·사업자등록번호·통신판매업
신고번호 표시를 의무로 규정한다. **허위 표시는 미표시보다 불리하다.** 실제 값으로
교체하거나, 값이 준비될 때까지 서비스를 열지 않는다.

### [ ] 2. ⛔ 이용약관 · 개인정보처리방침이 존재하지 않는다

> **진행 상황 (396bd87):** `/terms`, `/seller-terms`, `/privacy`, `/refund-policy` 4종 초안을 코드에서 사실을 도출해 작성했다. 남은 것은 **빈칸 18개(`{{상호}}` 등)를 실제 값으로 채우는 일과 법무 검토** 뿐이다. 각 페이지에 '법률 검토 전 초안' 고지가 붙어 있다.

푸터의 `이용약관` `판매회원 이용약관` `개인정보 취급방침` 링크가 전부 `/customer`
(FAQ·공지 페이지)로 간다. 실제 약관 문서는 저장소 어디에도 없다.

이 앱은 사업자등록번호·대표자명·연락처·주소·계좌정보를 수집하고 회원탈퇴까지 처리한다.
개인정보보호법상 처리방침 공개는 의무다. 법무 검토를 거친 실제 문서가 필요하고,
각각 별도 경로(`/terms`, `/privacy`, `/seller-terms`)로 분리해야 한다.

### [ ] 3. ⛔ 청약철회 · 교환/반품 정책 고지가 없다

> **진행 상황 (396bd87):** `/refund-policy` 초안 작성, 결제 화면에서도 링크로 고지한다. 교환/반품 **기능** 자체는 여전히 미구현이라 '고객센터 접수'로 안내하고 있다.

전자상거래법상 고지 의무 사항인데 어디에도 없다. 주문 화면과 약관 양쪽에 필요하다.
참고로 코드상 교환/반품 **기능** 자체도 미구현이다(아래 27번).

### [ ] 4. "데모 사이트입니다" 문구가 남아 있다

- `src/components/Footer.tsx:35` — `copyright ⓒ tirezone. 데모 사이트입니다.`
- `src/app/layout.tsx:10` — metadata description 이 `...B2B 타이어 거래 플랫폼 데모`

실거래가 오가는 사이트가 스스로를 데모라고 표시하면 분쟁 시 불리하게 작용할 수 있다.

**단, 1~3번과 반드시 함께 처리할 것.** 법정 표시사항이 더미값인 상태에서 "데모"
문구만 떼면, 가짜 사업자등록번호를 진짜처럼 보이게 만드는 셈이라 오히려 나쁘다.
그래서 이번 작업에서는 일부러 손대지 않았다.

### [ ] 5. ⛔ Render 무료 플랜

`render.yaml` 에서 DB·웹 서비스 모두 `plan: free`.

- 무료 Postgres는 **30일 후 만료되고 데이터가 삭제**된다
- 무료 웹 서비스는 무활동 시 슬립 → 콜드스타트 수십 초

결제가 도는 서비스로 그대로 쓸 수 없다. 유료 전환 후, 내부 커넥션 문자열
사용 여부도 함께 재검토할 것(아래 31번).

---

## 🟠 개시 직전 — 설정하지 않으면 기능이 죽어 있다

### [ ] 6. Toss 웹훅 URL 등록 + `TOSS_WEBHOOK_SECRET` 설정

`src/app/api/payments/toss/webhook/route.ts` 는 시크릿이 없으면 **503만 반환한다**
(의도된 안전 기본값). 다음을 하지 않으면 웹훅이 한 번도 동작하지 않는다.

1. `TOSS_WEBHOOK_SECRET` 을 Render 환경변수에 설정
2. Toss 개발자센터에 `https://<도메인>/api/payments/toss/webhook?secret=<값>` 등록
3. 이벤트는 `PAYMENT_STATUS_CHANGED`, `CANCEL_STATUS_CHANGED` 선택

Toss는 웹훅에 서명을 하지 않는다. 인증은 URL에 심은 시크릿뿐이고, 실질 방어는
"본문을 신뢰하지 않고 Toss API로 재조회"하는 구조다. 시크릿은 액세스 로그에
남으므로 주기적 교체(= URL 재등록)가 필요하다.

### [ ] 7. CSP를 Report-Only → enforcing 으로 전환

> **진행 상황 (194b1fc):** 실제 테스트 결제를 돌려보니 Toss 공식 문서에 없는 도메인 3개가 나왔다 — `log.tosspayments.com`, `apigw-sandbox.tosspayments.com`, 그리고 결제창 iframe 인 `payment-gateway-sandbox.tosspayments.com`. 문서 목록대로 enforcing 했다면 결제창이 아예 안 떴을 것이다. 지금은 `*.tosspayments.com` / `*.toss.im` 와일드카드로 바꿔 위반 0건을 확인한 상태. **라이브 키로 한 번 더 확인한 뒤** 헤더 키를 `Content-Security-Policy` 로 바꾸면 된다.

`next.config.ts` 는 Toss가 공식 문서에 명시한 7개 오리진을 모두 허용하지만
아직 `Content-Security-Policy-Report-Only` 다. Toss 문서는 "이 호스트들이 도달
가능해야 한다"까지만 말하고 **어떤 CSP 지시자로 로드되는지는 명시하지 않는다.**

실제 결제 1건을 돌려 위반 리포트를 확인한 뒤 헤더 키를 `Content-Security-Policy`
로 바꿀 것. 검증 없이 켜면 결제창이 조용히 깨지고, 구매자에게는 아무 오류도 보이지 않는다.

### [ ] 8. 실 DB에 마이그레이션 적용 후 눈으로 확인

마이그레이션 3건이 **기존 데이터를 변경**한다. WASM Postgres로 시나리오 검증은
마쳤지만 실제 운영 데이터로는 돌려보지 않았다.

- `20260821090000_add_order_payment_deadline` — 30분 넘은 입금대기 주문이 첫 주문목록
  조회 시점에 **일괄 자동취소**된다(재고 복원). 이미 배송이 시작된 미결제 주문은 제외됨.
- `20260821093000_backfill_order_status_from_shipping` — 기존 주문의 `status` 를
  `shippingStatus` 에 맞춰 정렬
- `20260821103000_add_business_reg_number_unique` — 중복 사업자번호를 톰스톤 처리.
  **해당 계정은 탈퇴가 아니라 정상 활성 상태로 남는다.** `AdminActionLog` 에
  `BUSINESS_REG_NUMBER_TOMBSTONED` 로 기록되니 적용 후 반드시 조회해서 후속 조치할 것.

적용 전 백업 필수.

### [ ] 9. 상품 이미지 업로드 E2E 검증

`src/lib/server/storage.ts` 의 presigned POST 는 **실제 R2/S3 버킷에 대해 검증된 적이 없다**
(코드 주석에 명시). 판매자 계정으로 이미지 1장을 끝까지 올려 반환된 `url` 에서
보이는지 확인할 것. 버킷 CORS 설정도 함께 점검.

### [ ] 10. 알림 채널이 미구현이다

`src/lib/server/notify.ts` 의 활성 구현체는 아무것도 보내지 않는다. 즉:

- 구매자·판매자 가입 승인/반려를 **당사자가 알 수 없다** (가입은 PENDING 대기 상태)
- 주문 접수, 발송, 자동취소 통보 없음

호출 지점과 인터페이스는 완비돼 있으므로 이메일/SMS 제공자 키만 확보되면
구현체 교체 한 번으로 붙는다. 최소한 **가입 승인 통보**는 개시 전에 필요하다.

### [x] 11. 세션 만료 시간이 설정돼 있지 않다

> ✅ 완료 (03e30df) — 8시간 만료, 활동 시 갱신, jwt.maxAge 도 함께 맞춤.

`src/lib/server/auth.ts` 의 `session` 에 `maxAge` 가 없어 NextAuth 기본값(30일)이
적용된다. 금전 거래가 오가는 B2B 서비스에는 길다. 공용 PC 사용도 고려해 재검토할 것.

### [x] 12. `error.tsx` / `not-found.tsx` 가 없다

> ✅ 완료 (03e30df) — error/not-found/global-error 추가. 원문 에러·스택은 노출하지 않고 로그로만 보낸다.

App Router 에러 바운더리와 404 페이지가 전혀 없어서, 예외가 나면 Next 기본 화면이
그대로 노출된다. 최소한 루트에 두 개는 만들 것.

### [x] 13. 헬스체크 엔드포인트가 없다

> ✅ 완료 (03e30df) — `/api/health` 가 `SELECT 1` 로 DB 도달을 확인하고, render.yaml 의 healthCheckPath 에 연결. 실패해도 원인은 응답에 싣지 않는다.

Render가 인스턴스 상태를 판단할 근거가 없다. DB 연결까지 확인하는 가벼운
`/api/health` 를 추가하고 `render.yaml` 에 연결할 것.

### [ ] 14. 레이트리밋이 인메모리다

`src/lib/server/rateLimit.ts` — 재배포·재시작마다 초기화되고, 인스턴스를 늘리면
인스턴스 수만큼 허용량이 늘어난다. 로그인 브루트포스·가입 폭주·웹훅 위조 방어가
모두 여기에 의존한다. 스케일아웃 계획이 생기면 공유 저장소(Redis 등)로 교체할 것.
교체 지점은 `record`/`isBlocked`/`reset` 세 메서드뿐이다.

### [ ] 15. Prisma 커넥션 풀 설정이 없다

> **진행 상황 (03e30df):** README 운영 가이드에 기본 동작과 설정 방법을 문서화했다. 실제 값은 Render 플랜이 정해져야 결정할 수 있어 비워 둔 상태.

`DATABASE_URL` 에 `connection_limit` 이 지정돼 있지 않아 Prisma 기본값이 쓰인다.
Render 무료·저가 Postgres는 동시 연결 수가 적어 고갈될 수 있다. 플랜 확정 후
DB의 `max_connections` 에 맞춰 명시할 것.

---

## 🟠 UX — 기능은 있는데 사용자가 도달하거나 확인할 수 없다

전수 조사로 확인한 것들이다. 코드 결함이 아니라 **화면이 데이터를 안 보여주거나
진입점이 없는** 문제라 기능 테스트로는 잡히지 않는다.

### [ ] 16. 구매자가 송장번호를 볼 수 없다

판매자가 `송장번호`를 입력하고, `Order.trackingNumber` 에 저장되고, 관리자 화면에도
표시된다. 그런데 **구매자 화면 어느 곳에도 렌더링되지 않는다** — `trackingNumber` 를
출력하는 파일은 `admin/orders` 와 `seller/orders` 둘뿐이다.

배송이 핵심인 마켓플레이스에서 구매자가 운송장을 못 받는다. 택배사(`courier`) 역시
같다. 사이드바 메뉴 이름이 "주문내역 / **배송조회**"인데 배송 정보가 전혀 없다.

### [ ] 17. 결제·취소 버튼에 도달할 수 없다

- 사이드바 "주문내역 / 배송조회" → `/mypage/orders`. 이 화면에는 **결제하기·주문취소·
  구매확정 버튼이 하나도 없다** (조회 전용 표).
- 버튼이 있는 `/orders` 는 `SIDEBAR_LINKS` / `MYPAGE_LINKS` 어디에도 없고, `/main`
  에서만 링크된다.

결과: 주문 직후 자동 이동될 때를 놓치면, 구매자가 미결제 주문을 결제할 방법을
스스로 찾을 수 없다. 30분 뒤 자동 취소된다.

### [ ] 18. 여러 주문을 한 번에 결제하는 UI가 없다

`/orders` 의 결제 버튼은 `?orderId=<하나>` 로만 링크를 만든다. 체크박스나 선택 결제가
없다. 반면 `prepare` API 는 최대 100건을 한 결제로 묶을 수 있고 `/orders/pay` 는
`?orderIds=a,b,c` 를 받는다.

즉 **백엔드 기능에 진입점이 없다.** 실제로는 모든 주문이 1건씩 따로 결제되므로,
한 결제에 여러 주문이 걸리는 상황 자체가 거의 생기지 않는다 — 부분환불 로직이
사실상 발동하지 않는다는 뜻이다. 장바구니에 3개 담으면 결제도 3번 해야 한다.

### [ ] 19. 취소 사유를 구매자가 볼 수 없다

판매자가 취소할 때 사유 입력이 **필수**이고(`CANCEL_REASON_REQUIRED`),
`Order.cancelReason` 에 저장된다. 그런데 이 필드를 렌더링하는 화면이 하나도 없다.
구매자는 "입금후취소"라는 상태만 보고 왜 취소됐는지 알 수 없다. 자동 만료 취소도
마찬가지다 — "결제 시간 초과로 자동 취소되었습니다"를 기록만 하고 안 보여준다.

### [ ] 20. 구매자 화면의 날짜가 원본 ISO 문자열이다

`/mypage/orders` 와 `/orders` 가 `orderedAt` 을 가공 없이 출력해
`2026-08-24T02:09:37.334Z` 로 보인다. UTC라 한국 사용자에게는 **9시간 어긋난 시각**
이기도 하다. 관리자·판매자 화면은 `formatDate` 를 쓴다 — 구매자 화면만 빠졌다.

### [ ] 21. 로딩 상태를 쓰지 않아 "없음"이 먼저 보인다

`useOrders()` 가 `loading` 을 제공하는데 `/mypage/orders`, `/orders`,
`/mypage/status`, `/wishlist` 어디도 쓰지 않는다. 데이터가 도착하기 전 잠깐
"총 0건" 과 빈 표가 보였다가 채워진다. `/mypage/orders` 에는 빈 상태 문구조차 없어
주문이 없으면 헤더만 있는 표가 남는다.

### [ ] 22. 목록에 검색·필터·페이지네이션이 없다

`admin/orders`, `seller/orders`, `mypage/orders` 모두 전체를 한 번에 렌더링한다.
주문이 쌓이면 그대로 느려지고 찾을 수도 없다. 카탈로그는 서버 페이징으로 바꿨지만
주문 목록은 그대로다.

---

## 🟡 개선 — 지금 당장 막히지는 않지만 남는 부채

### [ ] 23. 비밀번호 재설정 confirm 엔드포인트에 레이트리밋이 없다

`POST /api/account/password-reset/confirm` 는 인증 없이 호출 가능한데
`src/lib/server/rateLimit.ts` 의 리미터를 쓰지 않는다. request 쪽에는 걸려 있다.

토큰이 256비트 난수라 무차별 대입은 사실상 불가능하고, `consumePasswordReset`
이 토큰 조회를 먼저 해서 무효 토큰이면 bcrypt에 도달하기 전에 끊기므로 CPU
증폭도 없다. 그래서 심각도는 낮다. 다만 로그인 경로가 같은 이유로 리미터를 두고
있으므로 일관성 차원에서 붙이는 편이 낫다.

### [ ] 24. 주문별 환불 원장이 없어 관리자 배지 금액이 부정확하다

부분환불은 자동화됐지만, **어느 주문의 환불이 실제로 성공했는지**를 기록하는
원장이 없다. `Payment.refundAmount` 는 "취소된 주문 금액의 누계"일 뿐 "아직 덜 나간
금액"이 아니다.

실측 예: 100,000 / 50,000 / 30,000 주문 3건 중 첫 건의 Toss 호출만 실패한 경우 —
`/admin/orders` 배지는 `환불 필요 · 180,000원` 을 표시하지만 실제 미해결액은
100,000원뿐이다.

과다환불로는 이어지지 않는다(Toss가 잔액 초과 요청을 거부한다). 하지만 운영자가
배지 금액을 그대로 입력하면 요청이 거부되고 혼란이 생긴다. README 운영 가이드 §1에
"콘솔의 `balanceAmount` 를 기준으로 삼으라"는 경고를 넣어 두었으나, 근본 해결은
주문 단위 환불 원장(성공/실패/금액/`transactionKey`)을 두는 것이다. 스키마 변경 건.

---

### [ ] 25. `bankName` 이 자유 텍스트다

Toss는 은행·카드사를 코드로 다룬다(`06`/`KOOKMIN`, `88`/`SHINHAN` — 기관 코드 문서).
`prisma/schema.prisma` 의 `User.bankName` 은 자유 문자열이라 매핑이 불가능하다.

지금은 표시용이라 문제없지만, **가상계좌 환불(`refundReceiveAccount.bank`)이나
예금주 실명조회를 붙이는 순간 막힌다.** 스키마 변경 건이므로 그 기능과 함께 처리할 것.

### [x] 26. `/reset-password/admin` 이 proxy 보호 범위 밖이다

> ✅ 완료 (03e30df) — matcher 를 확장했다. `/reset-password`, `/reset-password/confirm`, `/find-id` 는 공개로 유지 — 잠그면 비밀번호를 잊은 사용자가 영구 차단된다.

`src/proxy.ts` 의 matcher 는 `/seller/:path*`, `/admin/:path*` 뿐이다.
해당 페이지는 클라이언트 `RequireAuth allow={["ADMIN"]}` 와 서버 API 의
`requireRole(["ADMIN"])` 로 막혀 있어 **실제 취약점은 아니지만**, 페이지 셸이
누구에게나 서빙되어 일관성이 없다. `/admin` 하위로 옮기거나 matcher 를 넓힐 것.

### [ ] 27. 교환/반품 플로우가 없다

`CANCEL_STATUS` 의 `교환완료` `반품완료` `재고없음` `상품미도착` 을 세팅하는 코드가
전혀 없다. 마이페이지의 취소/교환/반품 현황에서 이 4칸은 항상 0이다.
구매자 신청 → 판매자/관리자 처리 화면이 신규 구현으로 필요하다.

### [ ] 28. 결제수단이 카드 전용인데 상태명은 입금 기준이다

`src/app/orders/pay/page.tsx:100` 이 `method: "CARD"` 로 고정돼 있는데,
주문 상태는 `입금대기` / `입금완료` 다. 계좌이체·가상계좌를 쓰는 것처럼 읽힌다.
결제수단을 넓히거나 문구를 카드 기준으로 정리할 것.
가상계좌를 추가한다면 환불 시 `refundReceiveAccount` 가 필수가 되므로 25번과 함께 봐야 한다.

### [ ] 29. 이벤트·공지·FAQ·굿즈가 목데이터다

`src/lib/mockData.ts` 하드코딩. 관리자 화면(`/admin/events`)도 읽기 전용이라
운영자가 바꿀 수 없다. 금전 관련 화면은 실데이터로 전환했지만 이쪽은 남겨뒀다.

### [x] 30. `robots.txt` / `sitemap` 이 없다

> ✅ 완료 (03e30df) — app/robots.ts. 운영·인증 경로는 차단하되 가입 진입점(`/signup`, `/seller/signup`)은 색인 허용.

사업자 전용 서비스라 색인 정책을 명시적으로 정하는 편이 낫다.

### [ ] 31. 배포 파이프라인

- `render.yaml` 의 `buildCommand` 안에서 `prisma migrate deploy` 가 돈다.
  빌드가 마이그레이션 이후 실패하면 스키마만 앞서 나간다.
- `DATABASE_URL` 이 `externalConnectionString` 이라 내부망 대신 공용 인터넷을 경유한다.
  이전 커밋에서 의도적으로 선택한 값이라 임의로 되돌리지 않았으나, 유료 전환 시 재검토할 것.

### [ ] 32. 외부 검증 연동 미구현

- 예금주 실명조회 — `src/lib/server/bankVerification.ts` 인터페이스만 존재.
  계좌는 항상 **미인증**으로 저장된다.
- 사업자등록번호 진위확인(국세청) — 체크섬 검증까지만 구현. 형식이 맞아도
  실존하지 않는 번호는 통과한다(예: `0000000000` 은 체크섬상 유효).

---


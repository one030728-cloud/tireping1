# 작업 재개 지점

브랜치 `fix/code-review-11` 에서 진행한 실서비스 준비 작업의 중단 지점 기록.
남은 위험 목록은 `TODO.md`, 운영 절차는 `README.md` 의 운영 가이드를 볼 것.
이 문서는 **"어디까지 했고, 무엇을 실제로 확인했고, 다음에 뭘 하면 되는지"** 만 담는다.

---

## 1. 지금 어디까지 왔나

`ee14b4d` 이후 커밋 17개. 크게 다섯 갈래다.

| 갈래 | 내용 |
|---|---|
| 주문·재고 | 미결제 주문 30분 자동만료(재고 복원), 미결제·취소 주문 발송처리 차단, 주문상태를 배송축과 연동, 주문확인·구매확정 추가 |
| 결제 | Toss 웹훅, **부분환불 자동 제출**, 마지막 취소는 잔액 전체 취소, 팬텀 결제 제외, extraShipping 서버 도출 |
| 계정 | 비밀번호 재설정·아이디 찾기(운영자 매개), 탈퇴 시 PII 파기, 사업자등록번호 체크섬+unique, 비밀번호 변경 시 기존 세션 무효화 |
| 화면·문서 | 입출금/세금계산서/추가비용 실데이터화, 약관 4종 초안, 보안 헤더·CSP, 에러/404 페이지, 헬스체크, robots |
| 카탈로그 | 서버 페이징·필터·정렬, 판매자 정지 해제 |

---

## 2. 중단 시점에 진행 중이던 작업 ⚠️

**구매자 주문 관리 화면 재작성** (`TODO.md` 16~21번을 한 번에 해소).
중단 시점에 워킹 트리는 **깨끗**했다 — 결과물이 남아 있지 않다면 처음부터 다시 시키면 된다.

작업 범위:
- `/mypage/orders` 를 실제 주문 관리 화면으로 (상세 컬럼 유지 + 아래 전부 추가)
  - 배송상태·택배사·**송장번호** 노출 (현재 구매자는 운송장을 볼 수 없다)
  - 결제하기 / 주문취소 / 구매확정 버튼
  - **체크박스 다건 선택 결제** → `/orders/pay?orderIds=a,b,c`
  - 취소 사유 표시, 날짜 한국시간 포맷, 로딩·빈 상태
- `/orders` 는 `/mypage/orders` 로 리다이렉트 (`justOrdered` 파라미터 보존)

**반드시 지킬 함정 3개**

1. `/orders` 리다이렉트가 `/orders/pay`, `/pay/success`, `/pay/fail` 을 삼키면 **결제가 통째로 죽는다.** 정확히 `/orders` 만 매칭할 것. 빌드 출력에서 세 라우트가 살아있는지 확인.
2. 취소 버튼 조건은 `cancelOrder` 의 실제 가드와 일치해야 한다. 그 가드는 **두 축을 OR** 로 본다 — `orderStatusRank >= 배송중` **또는** `shippingStatus` 가 SHIPPED/DELIVERED. 한쪽만 보면 눌렀을 때 서버가 거부한다.
3. **송장 추적 URL을 지어내지 말 것.** 택배사 코드 매핑이 없어서 죽은 링크가 된다. 복사 편의만 제공.

이 작업이 끝나야 다건 결제 진입점이 생기고, 그래야 **부분환불이 실사용에서 발동한다.** 지금은 결제 링크가 항상 `?orderId=<하나>` 라 한 결제에 주문이 1건뿐이고, 부분환불 로직이 트리거될 일이 없다.

---

## 3. 재개하려면

```bash
cd C:\Users\hanwonchae\Desktop\tireping1
npm run dev          # 또는 .claude/launch.json 의 tirezone-dev
```

**환경변수** — `.env.local`(Next.js용)과 `.env`(Prisma CLI용) 두 개가 있고 둘 다 gitignore 대상.
`DATABASE_URL` 은 **양쪽 모두** 갖고 있으니 DB 주소를 바꿀 때는 두 곳 다 고쳐야 한다.

- `TOSS_CLIENT_KEY` / `TOSS_SECRET_KEY` — **API 개별 연동 테스트 키**(`test_ck_`/`test_sk_`).
  결제위젯 키(`test_gck_`/`test_gsk_`)를 넣으면 결제창이 아예 안 뜬다.
- `TOSS_WEBHOOK_SECRET` — 설정돼 있으나 **Toss 개발자센터에 URL 등록은 아직 안 했다.**
- `S3_*` — 비어 있다. 이미지 업로드만 503, 나머지는 정상 동작.

**DB** — Render Postgres(`singapore-postgres.render.com`). 실제 고객 데이터는 없고
시드 데모 계정 `admin` / `seller` / `buyer` 세 개뿐. 마이그레이션은 전부 적용 완료.

**테스트 데이터** — `[QA] 결제테스트 상품`(제조사 금호, 100/10R10, DOT 9000~9002)이
DB에 있고 현재 `HIDDEN` 이다. 결제 테스트 때 가격을 바꿔가며 재사용했다.
다시 쓰려면 `status` 를 `ACTIVE` 로 되돌리면 된다. 주문 이력이 걸려 있어 삭제하지 않았다.

---

## 4. 실물로 검증한 것 / 못 한 것

**실제 Toss 테스트 결제로 확인 완료**

- 6,000원 결제(3건 묶음) → 주문 3건 입금완료
- 1건만 취소 → `cancelAmount=1000`, `taxFreeAmount=0` 전송, 응답 `balanceAmount=5000`,
  화면 "부분환불 완료", 합계 5,000원
- 순차 취소 → 총 6,000원, 과다환불 0, 잔액 누락 0, 멱등키 주문별 분리
- **Toss는 잔액이 0이어도 `PARTIAL_CANCELED` 를 반환한다.** 응답 status를 그대로 쓰면
  전액 환불 후에도 "부분환불 완료"로 남는다 — 자체 판단(남은 활성 주문 0건)으로
  `CANCELED` 를 정하는 현재 설계가 맞다.
- **CSP**: 문서에 없는 도메인 3개가 실제 결제에서 나왔다 —
  `log.tosspayments.com`, `apigw-sandbox.tosspayments.com`,
  **`payment-gateway-sandbox.tosspayments.com`(결제창 iframe)**.
  enumerate 방식은 결제창을 막으므로 `*.tosspayments.com` / `*.toss.im` 와일드카드로 전환.
  재결제로 위반 0건 확인.
- 운영 DB에서 미결제 주문 2건이 로그인 시점에 자동 만료·재고 복원되는 것 확인.

**아직 실물 검증 못 한 것**

| | 왜 남았나 |
|---|---|
| **할부 결제 + 할부 부분취소** | 카드사 보안 프로그램이 Claude 브라우저 패널에서 안 잡힘. 평소 쓰는 Chrome에서 해야 함. 카드사가 할부 부분취소를 거부할 가능성이 있어 실물 확인 필요 |
| **Toss 웹훅 실제 페이로드** | 로컬이라 터널 필요(`npx ngrok http 3000`). **공식 문서에 페이로드 형식이 없어 방어적으로 파싱했고, 형식이 다르면 오류 없이 조용히 무시한다** — 실물로만 확인 가능 |
| 상품 이미지 업로드 | S3 미설정 |
| 라이브 키 환경의 CSP 도메인 | 계약 후 |

할부 테스트용으로 60,000원(10,000+20,000+30,000) 주문 세트를 만들었으나
30분 만료로 이미 취소됐을 것이다. `[QA]` 리스팅을 ACTIVE로 되돌려 다시 만들면 된다.

---

## 5. 검증 하네스 재현법

로컬에 Postgres도 Docker도 없다(WSL 배포판 미설치). WASM Postgres로 우회했다.

```bash
npm install --no-save @electric-sql/pglite @electric-sql/pglite-socket
```

- `package.json` / `package-lock.json` 은 오염되지 않는다. `npm ci` 하면 사라진다.
- **임베디드 모드**: 마이그레이션 SQL을 직접 적용해 데이터 시나리오를 검증할 때.
  `prisma.$queryRaw` 만 쓰는 코드는 이 방식으로 실제 실행까지 가능하다.
- **소켓 모드**(`PGLiteSocketServer`, 임의 포트): Prisma가 TCP로 붙어
  `prisma migrate deploy` 와 앱 코드를 그대로 돌릴 수 있다.
  단 **동시 연결 1개만 허용**하므로 `?connection_limit=1` 필수이고,
  실행마다 서버를 새로 띄워야 한다(재사용 시 `prepared statement "s0" already exists`).
- 검증 스크립트는 `prisma/` 아래에 두면 앱 TS 프로젝트에서 제외되어 `tsc` 를 오염시키지 않는다.
- Toss 호출은 `globalThis.fetch` 를 감싸 가로채면 요청 본문·멱등키·응답을 그대로 관찰할 수 있다.

이 방식으로 잡은 것: 마이그레이션 백필 누락, 배송완료 주문이 취소 가능해지던 회귀,
팬텀 결제 표시, 부분환불 성공인데 "처리중"으로 뜨던 문제, 비밀번호 변경 후 세션 유지.

---

## 6. 다음 순서

1. **진행 중이던 주문 관리 화면 마무리** (2절) — 끝나면 Opus가 diff 검토
2. **웹훅 실물 검증** — 할부보다 우선. 조용히 실패하는 유일한 항목
3. **할부 결제 + 부분취소** — 평소 쓰는 Chrome에서
4. 이후는 코드가 아니라 서류가 병목이다:
   사업자등록증 → 통신판매업 신고 → 약관 빈칸 18개 + 법무 검토 → Toss 라이브 계약

`TODO.md` 1~5번(법정 표시·약관·Render 플랜)이 실제 개시 차단선이고,
그중 넷은 코드로 해결할 수 없다.

---

## 7. 작업 방식 메모

서브에이전트 12개를 돌렸고 그중 6개에서 결함이 나왔다. **전부 빌드·타입체크·린트를
통과하면서 실데이터에서만 틀리는 종류**였다. 공통 패턴 둘:

- **기존 데이터를 고려하지 않음** — 마이그레이션 백필 누락, 기존 주문 상태 미정렬
- **지시를 과하게 적용** — "부분환불을 환불완료로 표시하지 말라" → 성공해도 영원히 "처리중"

그래서 에이전트 보고서를 그대로 신뢰하지 않고 diff를 직접 읽고, 돈이 얽힌 경로는
실제 Postgres로 시나리오를 돌려 확인하는 방식으로 진행했다. 같은 방식을 유지할 것.
